<!--
  Stubbed out — the model switcher was removed in favor of the
  server-side `GLM_MODEL` env var as the single source of truth. This
  file is kept as an empty component so any straggler `<ChatModelSelector />`
  reference (e.g. in a feature branch) compiles to a no-op instead of
  breaking the build. Delete this file once you're confident nothing
  references it.
-->
<template><span /></template>

<script setup lang="ts">
// Intentionally empty.
</script>
